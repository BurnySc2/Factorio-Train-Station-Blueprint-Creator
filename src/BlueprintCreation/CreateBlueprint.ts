import { defaultSettings } from "../constants/constants"
import { Blueprint } from "../constants/interfaces"

const createBlueprint = (blueprintSettings: typeof defaultSettings): Blueprint => {
    return {}
}

export default createBlueprint
